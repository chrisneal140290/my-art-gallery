// Site-wide feature flags
// Shop is hidden while the Shopify store (wr18gc-jx.myshopify.com) is
// suspended (HTTP 402 — subscription lapsed). Flip to true to restore
// the section and nav link exactly as they were.
export const SHOP_ENABLED = false;
